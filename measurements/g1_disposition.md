# G-1 — the four candidates, dispositioned

Client step, and G-1's retiring condition. **This document dispositions; it mints no numbered
entry** — the numbered §23 finding or §27 decision G-1's `Retires` names is the design side's and
is not mintable from this seat.

**The set was read from the decisions log's own section tail, NOT from decision 1081's entry.** The
entry is a headline and its rejected alternatives; the members' mechanisms, predictions, evidence
and dispositioning tests sit after the section's last numbered entry, behind another decision's
body. Filed with the manager and traced by them to a merge-ordering effect: the material was never
inside the entry, it only read as inside while that entry was last. **Recorded here so this
disposition does not silently assert a location for what it read.**

Every figure below is read from this repository's own published artefacts at
`magisteria_interface@5e3e7c5` — `measurements/unmet_read.json`,
`measurements/octave_clamp.json`, `measurements/octave_clamp_playa_sweep.json`. No engine run.

## Member 2 — detail-ladder truncation (the octave bound). **REFUTED.**

**Prediction was recorded before the artefacts were opened** and all five parts held.

The refutation does not rest on the size of the clamp's effect. It rests on **where the misses
are**:

| parent | clamp bites | worst relative change | `s` identical | riparian @4 m, @8 m |
|---|---|---|---|---|
| 100 m | no | **0.0** | yes | **OUT**, **OUT** |
| 200 m | no | **0.0** | yes | **OUT**, **OUT** |
| 400 m | no | **0.0** | yes | **OUT**, **OUT** |
| 800 m | no | **0.0** | yes | **OUT**, **OUT** |
| 1600 m | yes | 0.02954 | no | OUT, OUT |
| 3200 m | yes | 0.020698 | no | OUT, OUT |

**The cells that make condition 2 UNMET are outside band at four parents where the clamp provably
changes nothing** — zero movement, `s` bit-identical between shipped and lifted ladders. A cause
cannot produce an effect that is already fully present where the cause is absent.

The candidate's own counterfactual is the observed pattern's opposite on both axes it names:
coarse-parent-concentrated against misses at every parent; lift-sensitive against
`verdicts_that_moved: []`, `control_moved: []`, and `condition_2_verdict_moved = 0` over 100 seeds.

The movement-to-margin range quoted for this member **is not a row in the artefact** — it is a
derivation from `relative_change` and the band. Recomputed here from the rows: **0.0007–0.0109**,
reproducing the quoted range to the digit.

**Residual, explicitly not dispositioned here:** the dropped rungs live at 0.195–0.78 m
(`finest_reached` 0.39→0.195 at 1600 m, 0.78→0.195 at 3200 m), below every graded lag. This
refutes the member *as a cause of condition 2's verdict*. It says nothing about what a viewer
standing at walking scale sees, and must not be cited for that.

## Member 1 — producer-side gate divergence at playa (the closure standard). **NOT REFUTED; the live candidate for playa's cells.**

Its predicted signature is present in full, and it is a conjunction, not a single statistic:

- **Positive slope gap at every parent** — +0.1137 … +0.2123 across the 32× span, sign-stable.
- **The every-parent miss sits at playa's NARROWEST band** — 16 m, `band_width_over_mean` 0.4136,
  against 0.7046 at 8 m and 0.8549 at 4 m. A *per-lag* gate is strict exactly there.
- **Rank 1 of 5 on closure scatter** (0.7683, n=6) while narrowest where the per-lag gate reads —
  the wide/narrow discordance the mechanism requires.
- **It reaches riparian_margin not at all**, whose gap is negative and whose misses are at the two
  *widest* lags — the opposite signature.

**Disposition: live, and its remaining test is mine and artefact-only** — re-grade playa's cells
under a slope-aware closure on the published rows. The signature check above is that test's first
half and it passes; the re-grade itself is not run here and this document does not claim it.

## Members 3 and 4 — the riparian_margin band's derivation, and the fine-lag surface. **BOTH HELD. NEITHER REFUTED. THE CHOICE IS NOT MINE.**

Both predict persistence across seeds and parents. Both are satisfied by the same evidence:
outside band at 4 m and 8 m at **all six** parents, miss factors 3.15–3.54× and 1.37–1.58×,
`log_slope_gap` −0.4438 … −0.5557 negative throughout, and outside band in **100 of 100** seeds.

**They are degenerate on every measurement this repository can take.** Member 3's test re-derives
the band at wider support — the producing side's lane. Member 4's is engine-side, behind another
roadmap item. Nothing in this repository discriminates them, and decision 1076 leaves the reading
open. **Reporting the degeneracy is the disposition; choosing between them would be a ruling I was
not asked for and cannot ground.**

## What this accounts for, as `Retires` requires

**Three cells rather than two strata** — member 1 reaches `playa@16 m`; members 3/4 reach
`riparian_margin@4 m` and `@8 m`. No member needs "strata" to state its prediction, and the fourth
cell (`playa@8 m`, outside at two of six parents and crossing in 11 of 100 seeds) is the marginal
one that belongs to member 1's mechanism and to the arming clause, not to a stratum reading.

**Two exponent errors of opposite sign, stable across the 32× span** — playa positive (member 1),
riparian_margin negative (members 3/4). Every-parent and every-seed sign stability is what
band-side and surface-side causes produce; seed noise does not.

## What is NOT dispositioned

The held riparian_margin reading (members 3 vs 4). The octave bound's sub-metre residual. Any
verdict: the six unfailable cells stay ruled, the bound stays at 14, and **condition 2 stays
UNMET** — nothing here moves it. The arming clause on condition 2 is untouched and still binds.
