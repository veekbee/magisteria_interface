{
  "_form": "The claim about decision 985's two published artefacts. The files are DATA; this is the claim about them. Both are COMMITTED, not fetched -- 15.9 KB and 204 KB against decision 948's ~10 MB threshold -- so absent is a failure here and not a working clone.",
  "artefact": "decision 985's detail-function rows, the conformance vectors for `d(x, y)`, and decision 1019's measured landform bands",
  "ruled_by": [
    "decision 985",
    "decision 986",
    "decision 1004",
    "§16.5.1",
    "§23.977"
  ],
  "source": {
    "repo": "git@github.com:veekbee/magisteria.git",
    "path": "data/detail_rows_output, data/detail_conformance_output, data/calibration_rows_1019_output",
    "_reference_implementation": "sim/detail.py is authoritative and this client conforms to it. The vectors are how that conformance is checked rather than asserted: 300 positions the producing side chose, each carrying the float64 BIT PATTERN of its position and its expected value."
  },
  "produced_by": "tools/build_detail_rows.py, tools/emit_detail_conformance.py, tools/build_calibration_rows_1019.py",
  "source_commit": "09396cb",
  "vendored_by": "copied verbatim; no transform, so no vendor tool of its own",
  "_amended": "vendored three times in two days and each re-cut closed a defect this boundary found. 060cddd published `residual_rms` for one parent lattice where this client refines two; 4af865c added `residual_rms_by_parent_m` and, in the same cut, regressed the top-level placeholder warning to null and still shipped 88 bare `NaN` tokens; 85626b3 carries the lattice corner as `origin_hex`, emits with NaN refused at the source, and declares the placeholder state rather than inferring it from a key. No vector value moved across any of the three. 31efcab is the fourth and the first to move values rather than close a defect: every value-bearing scalar now carries its bit pattern in decision 985's nested `{dec, hex}` form, the parallel `origin`/`origin_hex` convention is retired, and `content_digest` arrives in both artefacts. The amplitudes are still placeholders -- decision 1019's calibrated values ride the second re-vendor. 09396cb is the FIFTH and the one the fourth promised: the amplitudes stop being placeholders. `spectral_slope` becomes the ground's own exponent fitted over decision 1033's settled 1-60 m window on raw ground, and `amplitude_m` the measured amplitude through a per-row constant that converts backlog 229's median second difference into the sd of the detail term this client adds. A THIRD FILE arrives with them: decision 1019's measured landform bands, whose `window_support` reads `joint_membership_per_point_1019` -- the re-sourced windows, not the catchment-median twelve -- which is what clears this client's `WRONG_SUPPORT` refusal on the band set's own declaration rather than on an assurance. The conformance vectors moved twice between dispatch and landing: once for the value state, once because they now carry 300 vectors and four property audits at EVERY parent this pyramid refines instead of at the shipped one alone. The digest named in the re-vendor dispatch was superseded before it was vendored, which is why the bytes here are checked against the commit and not against a message.",
  "files": {
    "detail_rows.json": "ff3f01a9bf824ccaf1d0fe49d923a8a49c4f2fde9c2520da86467b76a0ef9101",
    "detail_conformance.json": "6eb34b7f1bcfa567644d7cf2f5a576d06adbc1955cceabe1d7045bba6f51ed76",
    "calibration_rows_1019.json": "e1777722251db58d6b84c1a3ce39fcf0e7dd38e65bcb3c7d36cf88075cc4eb5b"
  },
  "cross_repo": {
    "_what": "how each file above is checkable against `source_commit` in a simulation checkout. Both are byte copies: nothing is transformed on the way in, which is the point of vendoring a reference's own output.",
    "files": {
      "detail_rows.json": {
        "upstream": "data/detail_rows_output/detail_rows.json",
        "how": "bytes"
      },
      "detail_conformance.json": {
        "upstream": "data/detail_conformance_output/detail_conformance.json",
        "how": "bytes"
      },
      "calibration_rows_1019.json": {
        "upstream": "data/calibration_rows_1019_output/calibration_rows_1019.json",
        "how": "bytes"
      }
    }
  },
  "_the_corner_is_carried_as_bits": "`parent.origin_hex`, in BOTH artefacts' parent blocks, which the gate compares against each other rather than reading whichever one it happens to want. It is there because this engine's string-to-double reads the corner's decimal to the farther of its two neighbouring doubles -- 2.22e-10 m out against a correct 1.10e-11 m -- which moves every parent node and fails property 2 in float64. Nine orders below the float32 render quantum, so nothing drawn is affected. The DRAWN lattice still takes its corner from `terrain_export.json`'s decimal, deliberately: the heightfield's, the tile grid's and the detail field's corners are all wrong by the same ulp and therefore consistent, and consistency across the three is what the renderer depends on. Both move together at the export's next re-emission, or neither.",
  "vendored_against_commit": "09396cbdd2d4bd0e4e5d0f8d8e27bcb0a5c95e51",
  "content_digest": {
    "_what": "each artefact's own digest over itself with its emission stamp removed, so a re-emission that moved no value says so. Recorded here as well as in the file because the question a consumer asks -- did anything I depend on change -- is asked BEFORE reading the file, and answering it by diffing two copies is the work this field exists to avoid.",
    "_not_the_same_as_files": "`files` is the sha256 of the BYTES and covers the emission stamp; these cover the content and do not. Both are here because they answer different questions, and a PIN carrying only one of them makes the other a diff.",
    "files": {
      "detail_rows.json": "sha256:704edb7c24f2f8891bd18a16cecbedaa64f1506f9577f37aa87b648dd125d2a2",
      "detail_conformance.json": "sha256:b968fab9738456b696bd66707aea9e9f2d0542d805391c9cbd591588f0a02938",
      "calibration_rows_1019.json": "sha256:1cf204053687cda19bdfab73c0494d1b67e147a89960d8c0ea8b4c409cd6da1a"
    }
  },
  "_values_state": "the rows declare `_values_landed` and no longer carry `_values_are_placeholders` with a warning beside a sentence saying the numbers are unsettled. An earlier cut carried both at once and this client's gate refused it: a reader could not tell whether it held measurements. The two states take different keys now and only one can be present."
}
