{
  "_form": "The claim about decision 985's two published artefacts. The files are DATA; this is the claim about them. Both are COMMITTED, not fetched -- 15.9 KB and 204 KB against decision 948's ~10 MB threshold -- so absent is a failure here and not a working clone.",
  "artefact": "decision 985's detail-function rows and the conformance vectors for `d(x, y)`",
  "ruled_by": [
    "decision 985",
    "decision 986",
    "decision 1004",
    "\u00a716.5.1",
    "\u00a723.977"
  ],
  "source": {
    "repo": "git@github.com:veekbee/magisteria.git",
    "path": "data/detail_rows_output, data/detail_conformance_output",
    "_reference_implementation": "sim/detail.py is authoritative and this client conforms to it. The vectors are how that conformance is checked rather than asserted: 300 positions the producing side chose, each carrying the float64 BIT PATTERN of its position and its expected value."
  },
  "produced_by": "tools/build_detail_rows.py, tools/emit_detail_conformance.py",
  "source_commit": "85626b3",
  "vendored_by": "copied verbatim; no transform, so no vendor tool of its own",
  "_amended": "vendored three times in two days and each re-cut closed a defect this boundary found. 060cddd published `residual_rms` for one parent lattice where this client refines two; 4af865c added `residual_rms_by_parent_m` and, in the same cut, regressed the top-level placeholder warning to null and still shipped 88 bare `NaN` tokens; 85626b3 carries the lattice corner as `origin_hex`, emits with NaN refused at the source, and declares the placeholder state rather than inferring it from a key. No vector value moved across any of the three.",
  "files": {
    "detail_rows.json": "71a551fe611137fa65e994f1f64bf77302e05196ee2a38cadfb93dccab177a92",
    "detail_conformance.json": "f75de5d9dd7559987f949844353cfdd4eb5c4fd7c852b15401397d000e071dd2"
  },
  "cross_repo": {
    "_what": "how each file above is checkable against `source_commit` in a simulation checkout. Both are byte copies: nothing is transformed on the way in, which is the point of vendoring a reference's own output.",
    "files": {
      "detail_rows.json": {
        "upstream": "data/detail_rows_output/detail_rows.json",
        "how": "bytes"
      },
      "detail_conformance.json": {
        "upstream": "data/detail_conformance_output/detail_conformance.json",
        "how": "bytes"
      }
    }
  },
  "_the_corner_is_carried_as_bits": "`parent.origin_hex` in the conformance artefact. It is there because this engine's string-to-double reads the corner's decimal to the farther of its two neighbouring doubles -- 2.22e-10 m out against a correct 1.10e-11 m -- which moves every parent node and fails property 2 in float64. Nine orders below the float32 render quantum, so nothing drawn is affected. The rows artefact's own `parent` block does NOT carry it; the gate builds the conformance lattice from the conformance artefact, so that costs nothing today and is worth closing when the two blocks next move."
}
